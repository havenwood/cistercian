<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Convert numbers to 13th century Cistercian numerals. A single glyph represents numbers 0-9999.">
  <link rel="icon" href="data:,">
  <title>Cistercian Numerals</title>
  <script defer src="https://unpkg.com/htmx.org@2.0.8"></script>
  <script defer src="https://unpkg.com/idiomorph@0.7.4/dist/idiomorph-ext.min.js"></script>
  <style>
    :root {
      --bg: #1a1a2e;
      --bg-input: #2a2a4e;
      --border: #4a4a6a;
      --accent: #7a7aaa;
      --accent-muted: rgba(122, 122, 170, 0.5);
      --accent-glow: rgba(122, 122, 170, 0.3);
      --parchment: #f5f0e6;
      --parchment-glow: rgba(245, 240, 230, 0.25);
      --accent-alt: rgba(255, 180, 200, 0.4);
      --text: #eee;
      --text-muted: #aaa;
    }
    * { box-sizing: border-box; }
    body {
      font-family: system-ui, sans-serif;
      max-width: 900px;
      margin: 0 auto;
      padding: clamp(1rem, 4vw, 2rem);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }
    h1 {
      text-align: center;
      font-size: clamp(1.25rem, 4vw, 2rem);
      color: var(--parchment);
      text-shadow: 0 0 12px var(--parchment-glow);
    }
    .input-wrapper {
      position: relative;
      width: 100%;
    }
    .input-wrapper input {
      width: 100%;
      padding: clamp(0.75rem, 2vw, 1rem);
      font-family: ui-monospace, monospace;
      font-size: max(16px, clamp(1.1rem, 2.5vw, 1.35rem));
      line-height: 1.4;
      border: 2px solid var(--border);
      border-radius: 8px;
      background: transparent;
      color: transparent;
      caret-color: var(--text);
      white-space: pre;
      overflow: hidden;
      position: relative;
      z-index: 1;
      -webkit-appearance: none;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      touch-action: manipulation;
    }
    .input-wrapper input::placeholder {
      color: var(--text-muted);
    }
    .input-wrapper input.hidden-text {
      -webkit-text-security: disc;
      text-security: disc;
    }
    .input-wrapper input:focus {
      outline: none;
      border-color: var(--accent);
      box-shadow: 0 0 8px var(--accent-glow);
    }
    .input-wrapper input[type="search"]::-webkit-search-decoration,
    .input-wrapper input[type="search"]::-webkit-search-cancel-button,
    .input-wrapper input[type="search"]::-webkit-search-results-button,
    .input-wrapper input[type="search"]::-webkit-search-results-decoration {
      -webkit-appearance: none;
      display: none;
    }
    #input_backdrop span.glow {
      animation: text-glow 2s ease-out;
    }
    @keyframes text-glow {
      from {
        text-shadow: 0 0 8px var(--parchment), 0 0 16px var(--accent);
        filter: drop-shadow(0 0 4px var(--accent));
      }
      to {
        text-shadow: none;
        filter: none;
      }
    }
    #input_backdrop {
      position: absolute;
      inset: 0;
      padding: clamp(0.75rem, 2vw, 1rem);
      font-family: ui-monospace, monospace;
      font-size: max(16px, clamp(1.1rem, 2.5vw, 1.35rem));
      line-height: 1.4;
      border: 2px solid transparent;
      background: var(--bg-input);
      border-radius: 8px;
      pointer-events: none;
      white-space: pre;
      overflow: hidden;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      contain: strict;
    }
    #input_backdrop .num-a {
      color: var(--text);
      text-decoration-line: underline;
      text-decoration-style: dotted;
      text-decoration-color: var(--accent-muted);
      text-decoration-thickness: 2px;
      text-underline-offset: 4px;
      -webkit-text-decoration-line: underline;
      -webkit-text-decoration-style: dotted;
      -webkit-text-decoration-color: var(--accent-muted);
    }
    #input_backdrop .num-b {
      color: var(--text);
      text-decoration-line: underline;
      text-decoration-style: dotted;
      text-decoration-color: var(--accent-alt);
      text-decoration-thickness: 2px;
      text-underline-offset: 4px;
      -webkit-text-decoration-line: underline;
      -webkit-text-decoration-style: dotted;
      -webkit-text-decoration-color: var(--accent-alt);
    }
    #input_backdrop .sep {
      color: var(--text-muted);
    }
    #input-hint {
      display: block;
      margin-top: 0.5rem;
      padding-left: calc(clamp(0.75rem, 2vw, 1rem) + 2px);
      color: var(--text-muted);
      transition: opacity 2s ease-out;
    }
    #input-hint.faded {
      opacity: 0;
    }
    .hint-basingstoke {
      display: none;
    }
    body.system-basingstoke .hint-cistercian {
      display: none;
    }
    body.system-basingstoke .hint-basingstoke {
      display: inline;
    }
    .system-selector {
      display: flex;
      gap: 0;
      border: none;
      padding: 0;
      margin: 0 auto 1.5rem;
      justify-content: center;
    }
    .system-option {
      position: relative;
      cursor: pointer;
    }
    .system-option input {
      position: absolute;
      opacity: 0;
      width: 0;
      height: 0;
    }
    .system-option span {
      display: block;
      padding: 0.5rem 1rem;
      background: var(--border);
      color: var(--text-muted);
      transition: background 0.2s, color 0.2s;
      font-size: clamp(0.9rem, 2vw, 1rem);
    }
    .system-option:first-child span {
      border-radius: 8px 0 0 8px;
    }
    .system-option:last-child span {
      border-radius: 0 8px 8px 0;
    }
    .system-option input:checked + span {
      background: var(--accent);
      color: var(--text);
    }
    .system-option input:focus-visible + span {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }
    .controls {
      position: relative;
    }
    .secret-toggle {
      position: absolute;
      right: 0;
      top: clamp(-2.75rem, -5.5vw, -3.25rem);
      cursor: pointer;
    }
    .secret-toggle input {
      position: absolute;
      opacity: 0;
      width: 0;
      height: 0;
    }
    .secret-toggle span {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 34px;
      height: 34px;
      background: var(--border);
      color: var(--text-muted);
      border-radius: 8px;
      transition: background 0.2s;
      font-size: 18px;
    }
    .secret-toggle span::before {
      content: '\1F435';
    }
    .secret-toggle input:checked + span {
      background: var(--accent);
    }
    .secret-toggle input:checked + span::before {
      content: '\1F648';
    }
    .secret-toggle input:focus-visible + span {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }
    .orientation-toggle {
      position: absolute;
      left: 0;
      top: clamp(-2.75rem, -5.5vw, -3.25rem);
      display: flex;
      gap: 0;
      border: none;
      padding: 0;
      margin: 0;
    }
    .orientation-toggle label {
      cursor: pointer;
    }
    .orientation-toggle input {
      position: absolute;
      opacity: 0;
      width: 0;
      height: 0;
    }
    .orientation-toggle label > span {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 34px;
      height: 34px;
      background: var(--border);
      color: var(--text-muted);
      transition: background 0.2s, color 0.2s;
      font-size: 18px;
    }
    .orientation-toggle label:first-child > span {
      border-radius: 8px 0 0 8px;
    }
    .orientation-toggle label:last-child > span {
      border-radius: 0 8px 8px 0;
    }
    .orientation-toggle input:checked + span {
      background: var(--accent);
      color: var(--text);
    }
    .orientation-toggle input:focus-visible + span {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }
    .icon-vertical, .icon-horizontal {
      display: inline-block;
      width: 12px;
      height: 18px;
      border: 2px solid currentColor;
      border-radius: 2px;
    }
    .icon-horizontal {
      width: 18px;
      height: 12px;
    }
    #numerals {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(115px, 1fr));
      gap: clamp(1rem, 3vw, 1.5rem);
      margin-top: clamp(1.5rem, 4vw, 2rem);
      view-transition-name: numerals-grid;
    }
    #numerals:has(figure:only-child) {
      grid-template-columns: min(calc(50% - 0.5rem), 200px);
      justify-content: center;
    }
    ::view-transition-old(numerals-grid),
    ::view-transition-new(numerals-grid) {
      animation-duration: 300ms;
      animation-timing-function: ease-out;
    }
    @keyframes focus-in {
      from { opacity: 0; transform: translateX(25px) scale(0.9); }
      to { opacity: 1; transform: translateX(0) scale(1); }
    }
    figure {
      margin: 0;
      text-align: center;
      animation: focus-in 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
    }
    figure img {
      width: 100%;
      aspect-ratio: 100 / 120;
      border-radius: 8px;
      transition: transform 0.3s ease;
    }
    .rotated figure img {
      transform: rotate(90deg) scale(0.92);
    }
    figcaption {
      margin-top: 0.5rem;
      font-size: clamp(1rem, 2vw, 1.2rem);
      color: var(--text-muted);
    }
  </style>
</head>
<body hx-ext="morph">
<%= yield %>
</body>
</html>
